{
  "name": "how-to-temu",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 8000",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1 --port 8000",
    "start": "astro build && astro preview --host 127.0.0.1 --port 8000",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@astrojs/markdown-satteri": "^0.4.2",
    "astro": "^7.3.4",
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=22.12"
  }
}
